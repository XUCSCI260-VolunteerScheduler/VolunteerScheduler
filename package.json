{
  "name": "volunteer-scheduler-demo",
  "version": "1.0.0",
  "description": "hopefully does what php will not",
  "scripts": {
    "start": "harp server --port $PORT"
  },
  "dependencies": {
    "harp": "*"
  }
}