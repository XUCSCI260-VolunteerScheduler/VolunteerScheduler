DROP TABLE STUDENT IF EXISTS;
CREATE MEMORY TABLE STUDENT(
  S_FNAME           TEXT NOT NULL,
  S_LNAME           TEXT NOT NULL,
  S_ID              TEXT NOT NULL, -- STUDENT ID
  S_MAJOR           TEXT,
  PRIMARY KEY(S_ID)
);

